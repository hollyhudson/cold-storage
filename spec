Pantry Manager


- App
	- Adding and removing
		- Categories
			- Item, quantitiy, color based on in or out?	
		
	- Finding
		- Categories
			- Items filtered by if we have it
		- Location

item name
in/out
quantity
category

Mental Model/Control Flow

Reactive:  Javascript parses user input (button presses, typed
text), and sends requests to Perl to update the database.  Perl
updates the database and then returns database content to javascript
so that it can update the UI.

item name: ________
category:  ________

protein . starch . fruit . vegetables
